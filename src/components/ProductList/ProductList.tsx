import styles from './ProductList.module.scss';
import cn from 'classnames';
import { ProductListProps } from './ProductList.props';

export const ProductList: React.FC<ProductListProps> = ({ className, ...props }) => {
    return (
        <ul>
            ProductList
        </ul>
    )
}